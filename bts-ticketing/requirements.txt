# BTS Ticketing Bot Dependencies

# 브라우저 자동화 (안티디텍션)
camoufox>=0.4.0          # Firefox 기반 안티디텍트 (권장)
nodriver>=0.38           # Chrome 기반 안티디텍트 (CDP 최소화)
seleniumbase>=4.32.0     # UC Mode (undetected-chromedriver)

# HTTP 클라이언트
aiohttp>=3.9.0           # 비동기 HTTP (CAPTCHA API 등)
httpx>=0.27.0            # 프록시 헬스체크
requests>=2.31.0

# Playwright 런타임 (현재 메인 실행 경로)
playwright>=1.41.0
playwright-stealth>=1.0.6
python-dotenv>=1.0.1

# AI 분석 (OpenClaw/Claude)
anthropic>=0.18.0

# NTP 시간 동기화
ntplib>=0.4.0

# CAPTCHA 자동 해결
2captcha-python>=1.2.0   # Turnstile 솔버 (권장)

# 프로세스 관리 (선택)
psutil>=5.9.0            # 브라우저 프로세스 정리

# Windows TTS (선택, Windows only)
# pywin32>=306
