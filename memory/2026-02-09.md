# 2026-02-09 (KST)

## Done (Ship)
- SOME 텔레그램 패널 E2E 제어 확인: Training→Unlock→Autopilot ON(10/30/60)→+10→Pause→Stop
- 텔레그램 버튼 UX: approve5/approve10 추가(훈련 단축)
- telegram-panel 감사로그(audit.jsonl) 지속
- SOME 중요 이벤트 알림(노이즈 최소) notifyCli 구현 + 크론 등록
- 크론/스크립트 에러 수정: zsh `status` read-only 변수 충돌 → OUT 변수 사용하도록 일부 크론 payload 수정
- KakaoTalk 대화 내보내기(txt) 기반 아바타화 플로우 검증
  - Google Drive 링크(uc export download)로 txt 다운로드 성공
  - 원문은 로컬 임시 분석 후 trash 삭제(A 모드)로 확정/준수
  - 통계 증류: `.state/derived/avatar-stats.json`
- 아바타 생활/행동 룰 15개를 사용자 확인 카드로 확정 (상대 전용)
  - 내부키: `mm1`, 표시명: `먀먀묭`
- telegram-panel에 파트너 메타데이터 레이어 추가
  - `some partner mm1|먀먀묭`
  - status 출력에 상대 표시
- 완벽성/프라이버시 잠금(Claude Code 패턴 차용)
  - `some/CLAUDE.md` 추가
  - `some/scripts/verify.sh` 추가(빌드+스모크)
  - git hooks: `.githooks/pre-commit` + `scripts/setup-githooks.sh`
    - 원문 import 파일/대용량(>5MB) 커밋 차단
    - verify 자동 실행

## In Progress
- `mm1` 룰/정책을 Autopilot 실제 동작에 강제 적용
  - 시간창(19–24), 딜레이 상한, 위기상황 승인 강제, 금지주제
- 시나리오 카드 20장 생성 + next/prev/approve/edit UX
- 온도계/관심도 리포트 MVP(지표+추세) 설계/구현
- Claude Code 리소스(subinium) 추가 리서치 후 적용(verify gate 확장 등)

## Decisions
- 개인정보: 원문 대화(txt) 기본 저장 OFF. 분석 후 증류/마스킹만 남기고 원문은 trash 삭제(A 모드).
- 상대 전용 아바타: `mm1`(내부키) + 표시명 분리(UI).
- 완벽성 우선: pre-commit + verify gate를 먼저 박고 기능 연결.
- 향후 개발: Step5 Kakao UI 자동화는 후순위(앱 설치/로그인/권한 준비 필요).

## Next (Tomorrow)
- `mm1 policy gate`를 runtime/panel 흐름에 E2E로 연결 + 테스트 고정
- 카드 20장 생성/진행 UX 완성
- 온도계/관심도 리포트 MVP + verify 스모크에 포함

## Notes
- cron.update가 간헐적으로 gateway timeout(10s) 발생 → 필요 시 timeoutMs 늘리거나 재시도.
