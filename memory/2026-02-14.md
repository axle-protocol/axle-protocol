# 2026-02-14 (Sat)

## SmartStore fulfillment dashboard
- Implemented SmartStore order XLSX import (passworded, encrypted) in admin UI + API.
  - Commit: 7eafa3c
  - UI: /admin (upload xlsx + password)
  - API: POST /api/admin/orders_xlsx_import (multipart; calls scripts/parse_smartstore_orders.py)
  - Persists to data/orders.json; auto-assign vendorId by productNo using mapping.json; returns imported/assigned/unassigned.
- Implemented SmartStore bulk shipping upload export (4 columns) generator.
  - Commit: 5fcbc9f
  - API: GET /api/admin/shipping_export.xlsx
  - Filters orders with trackingNumber+carrier; outputs sheet '발송처리' with 4 columns.
  - Supports chunking: ?chunk=N&size=K. Default method string: '택배,등기,소포'. Carrier labels: CJ대한통운 / 한진택배.
- Added admin UX to make MVP actually operable (no API-only dead ends).
  - Commit: 8825f5a
  - /admin now includes: shipping export download button + unassigned(vendorId null) order list + bulk assign vendor.
  - APIs: GET /api/admin/orders_unassigned, POST /api/admin/orders_assign
- Fixed tracking field compatibility between vendor UI and server data model.
  - Commit: 5cb17b3
  - Vendor POST tracking now writes both tracking{carrier,number} + legacy top-level carrier/trackingNumber.
  - Vendor GET orders normalizes legacy fields.
- Verified SmartStore “발주발송관리” encrypted template from 배송준비 tab (password 1234).
  - Sheet: 발주발송관리
  - Row1: instruction (delete before upload)
  - Row2 includes: 상품주문번호, 배송방법, 택배사, 송장번호
- Dashboard server kept alive with nohup.
  - PID example: 34323
  - Log: /tmp/automation-dashboard.log

## Instagram semi-automation (Claude Code output integrated)
- Integrated Claude Code delivered feature: Instagram post queue with Brand Guide + Validator + Scheduler.
  - Commit: bb1cb6c
  - New UI: /admin/instagram
  - Added ~9 admin APIs under /api/admin/ig/* (guide, create drafts, approve, reschedule, mark_sent, cancel, due-posts, etc.)
  - Purpose: semi-automation only (draft → approve → schedule → reminders), no credential storage / no auto-upload.
- Wrote Claude Code (Opus 4.6) prompt for next step: cardnews image generation (3~5) + caption blocks 200~300+ + package zip.
  - Doc commit: e1a3992
  - File: automation/dashboard/docs/claude-code-opus-ig-cardnews-imagegen-prompt.md
- Deep-research for KR IG references blocked until IG login on openclaw browser profile.
  - IG prompts: aim for “rorobeauty tone + thimbeauty luxury minimal visual”.

## X marketing
- Per rules: drafts only (Han approval required before posting).
- Draft main tweet idea (see below in chat or regenerate later).

## Notes
- GitHub push for docs branch failed earlier due to large binaries in repo (models/whisper/*.bin) exceeding GH limits.
